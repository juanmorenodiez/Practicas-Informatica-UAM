import numpy as np
import matplotlib.pyplot as plt
x = np. arange (0, 3 * np.pi , 0.1)
animals = ['cat ', 'dog ', 'monkey ']
y_sin = np.sin (x)

# El valor de dtype.name de la variable x es float64
# El valor de len() de la variable animals es 3
